import React from "react";
const SpSideBar = (props) => {
    return (
        <div className="spsidebar">
            {
                props.children
            }
        </div>
    )
}

export default SpSideBar